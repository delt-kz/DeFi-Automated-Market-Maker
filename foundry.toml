[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
gas_reports = ["AMM", "LendingPool", "LPToken", "MockERC20"]

[fuzz]
runs = 256

[invariant]
runs = 128
depth = 32
